diff --git a/drivers/input/keyboard/pinephone-keyboard.c b/drivers/input/keyboard/pinephone-keyboard.c
index d74ae97ed..f1fe2f397 100644
--- a/drivers/input/keyboard/pinephone-keyboard.c
+++ b/drivers/input/keyboard/pinephone-keyboard.c
@@ -39,7 +39,7 @@ MODULE_PARM_DESC(disable_input, "Disable the keyboard part of the driver");
 #define PPKB_DEFAULT_KEYMAP_COLS	12
 
 static const uint32_t ppkb_default_fn_keymap[] = {
-	KEY(0,  0, KEY_FN_ESC),
+	KEY(0,  0, KEY_F10),
 	KEY(0,  1, KEY_F1),
 	KEY(0,  2, KEY_F2),
 	KEY(0,  3, KEY_F3),
@@ -49,25 +49,55 @@ static const uint32_t ppkb_default_fn_keymap[] = {
 	KEY(0,  7, KEY_F7),
 	KEY(0,  8, KEY_F8),
 	KEY(0,  9, KEY_F9),
-	KEY(0, 10, KEY_F10),
-	KEY(0, 11, KEY_DELETE),
+	KEY(0, 10, KEY_F11),
+	KEY(0, 11, KEY_F12),
+
+	KEY(1,  0, KEY_ESC),
+	KEY(1,  1, KEY_HOME),
+	KEY(1,  2, KEY_UP),
+	KEY(1,  3, KEY_END),
+	KEY(1,  4, KEY_PAGEUP),
+	KEY(1,  5, KEY_SEMICOLON),
+	KEY(1,  6, KEY_APOSTROPHE),
+	KEY(1,  7, KEY_102ND),
+	KEY(1,  8, KEY_COMPOSE),
+	KEY(1,  9, KEY_SCROLLLOCK),
+	KEY(1, 10, KEY_RIGHTSHIFT),
+	KEY(1, 11, KEY_INSERT),
 
 	KEY(2,  0, KEY_SYSRQ),
-	KEY(2, 10, KEY_INSERT),
+	KEY(2,  1, KEY_LEFT),
+	KEY(2,  2, KEY_DOWN),
+	KEY(2,  3, KEY_RIGHT),
+	KEY(2,  4, KEY_PAGEDOWN),
+	KEY(2,  5, KEY_GRAVE),
+	KEY(2,  6, KEY_BACKSLASH),
+	KEY(2,  7, KEY_CAPSLOCK),
+	KEY(2,  8, KEY_YEN),
+	KEY(2,  9, KEY_RIGHTMETA),
+	KEY(2, 10, KEY_DELETE),
+
+	KEY(3,  0, KEY_KP0),
+	KEY(3,  1, KEY_KPDOT),
+	KEY(3,  2, KEY_KPCOMMA),
+	KEY(3,  3, KEY_KPENTER),
+	KEY(3,  4, KEY_KPEQUAL),
+	KEY(3,  5, KEY_LEFTBRACE),
+	KEY(3,  6, KEY_RIGHTBRACE),
+	KEY(3,  7, KEY_KPPLUS),
+	KEY(3,  8, KEY_KPMINUS),
+	KEY(3,  9, KEY_KPASTERISK),
+	KEY(3, 10, KEY_KPSLASH),
+
+	KEY(4,  1, KEY_KP1),
+	KEY(4,  4, KEY_KP3),
+	KEY(4,  6, KEY_KP6),
+	KEY(4,  8, KEY_KP9),
+	KEY(4,  9, KEY_KP7),
 
-	KEY(3,  0, KEY_LEFTSHIFT),
-	KEY(3,  8, KEY_HOME),
-	KEY(3,  9, KEY_UP),
-	KEY(3, 10, KEY_END),
-
-	KEY(4, 1, KEY_LEFTCTRL),
-	KEY(4, 6, KEY_LEFT),
-	KEY(4, 8, KEY_RIGHT),
-	KEY(4, 9, KEY_DOWN),
-
-	KEY(5, 2, KEY_FN),
-	KEY(5, 3, KEY_LEFTALT),
-	KEY(5, 5, KEY_RIGHTALT),
+	KEY(5,  2, KEY_FN),
+	KEY(5,  3, KEY_KP2),
+	KEY(5,  5, KEY_KP4),
 };
 
 static const struct matrix_keymap_data ppkb_default_fn_keymap_data = {
@@ -76,7 +106,7 @@ static const struct matrix_keymap_data ppkb_default_fn_keymap_data = {
 };
 
 static const uint32_t ppkb_default_keymap[] = {
-	KEY(0,  0, KEY_ESC),
+	KEY(0,  0, KEY_0),
 	KEY(0,  1, KEY_1),
 	KEY(0,  2, KEY_2),
 	KEY(0,  3, KEY_3),
@@ -86,7 +116,7 @@ static const uint32_t ppkb_default_keymap[] = {
 	KEY(0,  7, KEY_7),
 	KEY(0,  8, KEY_8),
 	KEY(0,  9, KEY_9),
-	KEY(0, 10, KEY_0),
+	KEY(0, 10, KEY_EQUAL),
 	KEY(0, 11, KEY_BACKSPACE),
 
 	KEY(1,  0, KEY_TAB),
@@ -112,7 +142,7 @@ static const uint32_t ppkb_default_keymap[] = {
 	KEY(2,  7, KEY_J),
 	KEY(2,  8, KEY_K),
 	KEY(2,  9, KEY_L),
-	KEY(2, 10, KEY_SEMICOLON),
+	KEY(2, 10, KEY_SLASH),
 
 	KEY(3,  0, KEY_LEFTSHIFT),
 	KEY(3,  1, KEY_Z),
@@ -124,13 +154,13 @@ static const uint32_t ppkb_default_keymap[] = {
 	KEY(3,  7, KEY_M),
 	KEY(3,  8, KEY_COMMA),
 	KEY(3,  9, KEY_DOT),
-	KEY(3, 10, KEY_SLASH),
+	KEY(3, 10, KEY_MINUS),
 
 	KEY(4,  1, KEY_LEFTCTRL),
 	KEY(4,  4, KEY_SPACE),
-	KEY(4,  6, KEY_APOSTROPHE),
-	KEY(4,  8, KEY_RIGHTBRACE),
-	KEY(4,  9, KEY_LEFTBRACE),
+	KEY(4,  6, KEY_KP5),
+	KEY(4,  8, KEY_KP8),
+	KEY(4,  9, KEY_RIGHTCTRL),
 
 	KEY(5,  2, KEY_FN),
 	KEY(5,  3, KEY_LEFTALT),
